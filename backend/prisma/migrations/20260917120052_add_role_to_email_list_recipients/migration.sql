BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[email_list_recipients] ADD [role] NVARCHAR(1000);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
